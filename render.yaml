services:
  - type: web
    name: ffmpeg-api
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python main.py

    
